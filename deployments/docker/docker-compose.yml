services:
  agentos:
    build:
      context: ../..
      dockerfile: deployments/docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - AGENTOS_API_KEY=
      - AGENTOS_WORKSPACE_ROOT=/app
